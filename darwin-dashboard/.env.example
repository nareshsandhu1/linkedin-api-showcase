# Darwin Dashboard — environment variables

# Postgres connection string.
# When omitted, the app uses seed fixture data for local development.
# DATABASE_URL=postgres://user:password@localhost:5432/darwin_dashboard

# Port the server listens on (default: 3001)
# PORT=3001

# Node environment
# NODE_ENV=development
